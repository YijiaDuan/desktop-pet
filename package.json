{
  "name": "desktop-pet",
  "private": true,
  "version": "0.1.0",
  "description": "Cozy desktop companions that live on your screen. First up: a goose.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:dmg": "tauri build --bundles dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  }
}
